[project]
name = "daftcli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.17",
    "click>=8.1.7",
    "getdaft>=0.3.2",
    "ray[client]>=2.35.0",
]

[project.scripts]
daftcli = "src.__main__:main"

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptool]
packages = ["daftcli"]
include_package_data = true

[tool.setuptools.packages.find]
exclude = ["daft_configs"]

[tool.setuptools.package-data]
daftcli = ["src/configs/*.yaml"]

[tool.pyright]
venvPath = '.'
venv = '.venv'

[tool.ruff]
include = ["src/**/*.py"]
