[project]
name = "daft_launcher"
dependencies = ["ray[default]", "boto3"]
license = { file = "LICENSE" }
requires-python = ">=3.9"
dynamic = ["version"]

[build-system]
requires = ["maturin"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
