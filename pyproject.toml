[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "daft_launcher"
requires-python = ">=3.9"
dependencies = ["ray[default]", "boto3"]

[tool.maturin]
bindings = "bin"
